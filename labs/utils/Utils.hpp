#include <string>
/// @brief Удаляет пробелы сначала и с конца строки. 
/// Если строка состоит только из пробелов, то присваивает s пустую строку.
/// @param s - ссылка на строку
void TrimSpaces(std::string& s);

void TrimAllSpaces(std::string& s);
